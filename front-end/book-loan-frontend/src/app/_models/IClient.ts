export interface IClient {
  id: number;
  cpf: string;
  name: string;
  address: string;
  city: string;
  neighborhood: string;
  number: string;
  phoneNumber: string;
  fixPhoneNumber: string;
}
